const wskaz = n => {
    if(n >= 100 && n <= 200){
        console.log("liczba znajduję się w przedziale");
    }
    else{
        console.log("liczba nie znajduję się w przedziale"); 
    }
}
let wynik = wskaz(200);
